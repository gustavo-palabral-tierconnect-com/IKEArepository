{
    "port": 1883,
    "host": "mqtt",
    "topic": "/IKEA/FLOW/DD",
    "mqtt-body": {
        "ledgerId": "${ledgerId}",
        "businessUnit": "${businessUnitName}",
        "process": "${processName}",
        "processStopTime": "${processStopTime}"
    }
}