{
    "port": 1883,
    "host": "mqtt",
    "topic": "/IKEA/FLOW",
    "mqtt-body": {
        "EPC": "${serialNumber}",
        "zone": "${zone.code}",
        "READER_IP": "${Reader_IP}",
        "lastDetectTime": "${lastDetectTime}"
    }
}